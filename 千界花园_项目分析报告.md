# 千界花园（BloomBloomGarden）项目全景分析报告

> 分析时间：2026-07（基于本地仓库 HEAD `cba8cc1`，与 GitHub `main` 同步）
> 仓库：https://github.com/yimeng2026/bloombloomgarden.git
> 分析方式：只读静态分析（未修改代码、未执行 git 写操作、未启动服务）

---

## 一、架构总览：一个仓库，两套并存的全栈体系

仓库当前实际包含**两个并行演进的全栈应用**，加上一个 Electron 桌面壳：

```
bloombloomgarden/
├── src/app + src/lib + prisma/     ← 体系 B：Next.js 16 应用（BloomBloomGarden + 学术研究生态）
├── backend/                        ← 体系 A：千界花园 v4.5 Express 后端（群智协同核心）
├── frontend/                       ← 体系 A：千界花园 v4.5 Vite + React 前端（70 页）
├── electron/                       ← 体系 A：Electron 桌面壳
└── （根目录混放 TOE-SYLVA 学术项目工作文件，与应用代码无关）
```

### 体系 A — 千界花园群智协同系统（v4.5 主线）
- **backend/**：`thousand-realms-garden-backend` v1.0.0，Express 4 + Prisma 5 + tsx，TypeScript
- **frontend/**：`thousand-realms-garden-frontend`，Vite + React + MUI 9 + Radix UI + Tailwind
- **electron/**：桌面端壳（electron-builder，appId `com.thousandrealms.garden`）
- v4.5 的 GatewayService / SwarmCoordinator / CanvasService / WorkflowService 均位于 `backend/src/services/`

### 体系 B — BloomBloomGarden + 学术研究生态（v4.5 之后的新增主线）
- 根 `package.json`：Next.js 16.2.9 + React 19.2.4 + Prisma 7（@prisma/adapter-libsql）+ Tailwind CSS 4
- 从 git 历史看，该体系在 v4.5 之后由「学术研究模块 v1.0 → 协作后端 v2.0 → 协作生态 v3.0 → 千年难题生态 → TOE-SYLVA 联动」系列提交逐步构建
- 部署目标：Railway（railpack）+ Docker + Vercel；README 描述的是这一体系

**两体系关系**：体系 A 是通用 Agent 群智协同控制台（重交互、重编排）；体系 B 是在同一仓库内长出的 Next.js 应用，起初是简化版 Agent 平台（README 描述），后期主要承载「学术研究协作生态」（SYLVA 数学研究 + GraphRAG + Ontology）。两者各自有独立的 Prisma schema 与数据库。

---

## 二、核心统计

| 维度 | 体系 A（backend/frontend） | 体系 B（Next.js src/app） |
|---|---|---|
| API 路由文件 | 39 个（`backend/src/routes/*.ts`） | **74 个**（`src/app/api/**/route.ts`） |
| HTTP 端点总数 | ≈302 个（router.METHOD 注册） | **133 个**（export GET/POST/PUT/DELETE） |
| 页面数 | **70 个**（`frontend/src/pages/*.tsx`），App.tsx 注册约 66 条路由 | **8 个**（`src/app/**/page.tsx`） |
| 数据模型 | 31 个（backend/prisma） | 46 个（prisma/schema.prisma） |
| 服务层 | 35 个服务文件（`backend/src/services/`） | 11 个库模块（`src/lib/`，约 4,600 行） |
| ORM/DB | Prisma 5 + SQLite | Prisma 7 + libsql adapter + SQLite |

### 与 v4.5 记录的对比
- v4.5 记录：「系统路由从约 30 个扩展至 **61 个**（20 静态页面 + 41 动态 API）」——对应体系 A 当时的前端路由表。
- 现状：体系 A 前端页面已增至 **70 个**（新增 HandoffPage、IntegrationsPage 等在 v4.5 之后的去 MOCK 阶段补入）；体系 A 后端为 39 个路由文件、约 302 个端点。
- 体系 B 的 Next.js API 已达 **74 个 route.ts / 133 个端点**——其中 `research/*` 独占 **52 个路由文件**，是 v4.5 之后增长的主体。仓库整体 API 面（两体系合计）约 435 个端点。
- 结论：61 → 74 的口径差异来自统计对象不同：61 是体系 A 前端路由数（现已 70 页）；74 是体系 B Next.js API 路由文件数。项目实际规模远超 v4.5 记录。

---

## 三、体系 B：Next.js API 端点全清单（74 路由 / 133 端点）

### agents（2 文件 / 5 端点）
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/agents | GET, POST | Agent 列表 / 创建（本地存储策略） |
| /api/agents/[id] | GET, PUT, DELETE | 单 Agent 详情（含 apiKey）/ 更新 / 删除 |

### chat（2 文件 / 2 端点）
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/chat | POST | 发送消息，SSE 流式响应（经平台适配器 platform-adapter） |
| /api/chat/group | POST | 群组蜂群协作聊天（7 种蜂群模式编排） |

### conversations（2 文件 / 4 端点）
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/conversations | GET, POST | 对话列表 / 新建对话 |
| /api/conversations/[id] | GET, DELETE | 对话详情（含消息）/ 删除 |

### groups（2 文件 / 5 端点）
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/groups | GET, POST | 群组列表 / 创建（支持嵌套子群组、蜂群模式、人工干预级别） |
| /api/groups/[id] | GET, PUT, DELETE | 群组详情 / 更新 / 删除 |

### workflows（2 文件 / 5 端点）
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/workflows | GET, POST | 工作流列表 / 创建（画布 JSON：节点+连线） |
| /api/workflows/[id] | GET, PUT, DELETE | 工作流详情 / 更新 / 删除 |

### graphrag（5 文件 / 6 端点）
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/graphrag/documents | GET, POST | 文档列表 / 上传并索引（分块+实体抽取） |
| /api/graphrag/entities | GET | 知识图谱实体查询 |
| /api/graphrag/relations | GET | 实体关系查询 |
| /api/graphrag/query | POST | GraphRAG 查询（社区摘要 + LLM 生成答案） |
| /api/graphrag/stats | GET | 图谱统计（文档/块/实体/关系/社区数） |

### ontology（7 文件 / 11 端点）
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/ontology/schemas | GET, POST | 本体 Schema 列表 / 创建 |
| /api/ontology/schemas/[id] | GET, PUT, DELETE | Schema 详情 / 更新 / 删除 |
| /api/ontology/instances | GET | 本体实例查询 |
| /api/ontology/relations | GET | 本体关系查询 |
| /api/ontology/extract | POST | 从文本/文档自动抽取本体实例（LLM） |
| /api/ontology/query | POST | 本体语义查询（LLM） |
| /api/ontology/stats | GET | 本体统计 |

### research（52 文件 / 95 端点）— 学术研究协作生态，体量最大

**基础数据与同步（8 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/init | POST | 初始化研究数据（含前端硬编码 fallback 迁移） |
| /api/research/stats | GET | 研究系统总统计 |
| /api/research/modules | GET | SYLVA Lean 模块列表（行数/sorry/定理数） |
| /api/research/papers | GET | 论文列表（Hilbert / Millennium 问题） |
| /api/research/notes | GET, POST, PUT, DELETE | 研究笔记全量 CRUD |
| /api/research/sync | POST | 从 TOE-SYLVA 仓库同步 Lean 代码结构 |
| /api/research/sylva-sync | GET, POST | SYLVA 同步状态查询 / 触发同步 |
| /api/research/verification | GET | 验证状态总览 |

**专家组 Panels（4 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/panels | GET, POST | 专家组列表（?domain 筛选）/ 创建 |
| /api/research/panels/[id] | GET, PUT, DELETE | 专家组详情 / 更新 / 删除 |
| /api/research/panels/[id]/members | GET, POST | 成员列表 / 添加（含预置专家模板） |
| /api/research/panels/[id]/execute | POST | 执行专家组任务（真实 LLM 调用） |

**研讨会 Workshops（4 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/workshops | GET, POST | 研讨会列表 / 创建（committee/debate/sequential/parallel） |
| /api/research/workshops/[id] | GET, PUT, DELETE | 详情 / 更新 / 删除 |
| /api/research/workshops/[id]/rounds | POST | 执行一轮研讨（集成真实 LLM，含共识度计算） |
| /api/research/workshops/[id]/conclude | POST | 结束研讨会并生成结论报告（LLM） |

**稿件流水线 Pipelines（5 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/pipelines | GET, POST | 流水线列表 / 创建（idea→outline→draft→review→…→publish） |
| /api/research/pipelines/[id] | GET, PUT, DELETE | 详情 / 更新 / 删除 |
| /api/research/pipelines/[id]/advance | POST | 推进到下一阶段（LLM 生成阶段产出+评分） |
| /api/research/pipelines/[id]/stages/[stageId] | GET, PUT | 阶段详情 / 更新（反馈、评分） |
| /api/research/pipelines/[id]/tasks | GET, POST | 流水线任务列表 / 创建 |

**评审团 Review Boards（5 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/review-boards | GET, POST | 评审团列表（domain/status/reviewMode 筛选）/ 创建 |
| /api/research/review-boards/[id] | GET, PUT, DELETE | 详情 / 更新 / 删除 |
| /api/research/review-boards/[id]/reviewers | GET, POST | 评审员列表 / 添加（含匿名编号） |
| /api/research/review-boards/[id]/reviews | GET, POST | 评审列表（targetId/decision/round 筛选）/ 提交评审 |
| /api/research/review-boards/[id]/reviews/stats | GET | 评审统计（均分、决定分布） |

**学术竞赛 Competitions（5 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/competitions | GET, POST | 竞赛列表 / 创建（tournament/elimination/round_robin） |
| /api/research/competitions/[id] | GET, PUT, DELETE | 详情 / 更新 / 删除 |
| /api/research/competitions/[id]/competitors | GET, POST | 参赛者列表 / 添加（预置模板） |
| /api/research/competitions/[id]/rounds | GET, POST | 轮次列表 / 执行一轮竞赛（LLM 评分排名） |
| /api/research/competitions/[id]/rounds/stats | GET | 竞赛统计 |

**导师制 Mentorships（3 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/mentorships | GET, POST | 辅导列表 / 创建（one_on_one/group/peer/socratic） |
| /api/research/mentorships/[id] | GET, PUT, DELETE | 详情 / 更新 / 删除 |
| /api/research/mentorships/[id]/sessions | GET, POST | 课程会话列表 / 执行会话（LLM 讲授+评分） |

**代码评审 Code Reviews（4 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/code-reviews | GET, POST | 评审组列表 / 创建（关联 Lean 模块/文件） |
| /api/research/code-reviews/[id] | GET, PUT, DELETE | 详情 / 更新 / 删除 |
| /api/research/code-reviews/[id]/reviewers | GET, POST | 评审员列表 / 添加 |
| /api/research/code-reviews/[id]/reviews | GET, POST | 逐行评审意见（severity 从 LLM 响应提取） |

**验证组 Validations（5 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/validations | GET, POST | 验证组列表 / 创建（numerical/symbolic/experimental/cross_reference/peer） |
| /api/research/validations/[id] | GET, PUT, DELETE | 详情 / 更新 / 删除 |
| /api/research/validations/[id]/validators | GET, POST | 验证员列表（role/specialty 筛选）/ 添加 |
| /api/research/validations/[id]/results | GET, POST | 验证结果列表 / 执行验证（LLM 提取结论） |
| /api/research/validations/[id]/results/stats | GET | 验证统计（verified/refuted/inconclusive 分布） |

**任务与其他（9 文件）**
| 路径 | 方法 | 功能 |
|---|---|---|
| /api/research/tasks | GET, POST | 学术任务列表 / 创建（prove/review/verify/discover/draft/critique/audit） |
| /api/research/tasks/[id] | GET, PUT, DELETE | 任务详情 / 更新 / 删除 |
| /api/research/tasks/stats | GET | 任务统计
 |
| /api/research/modes | GET | 协作模式清单（9 协作类型 × 20 协作方式） |
| /api/research/registry | GET | 协作注册表 |
| /api/research/registry/compatible | GET | 兼容协作方式匹配 |
| /api/research/collaborations/templates | GET | 预置协作模板列表 |
| /api/research/collaborations/init | POST | 一键初始化完整协作生态 |
| /api/research/millennium/init | GET, POST | 千年难题研究生态初始化 |

---

## 四、数据模型（46 个，prisma/schema.prisma，781 行）

### 核心协同域（8）
- **Agent**：LLM 供应商（llmProvider）、平台（agentPlatform）、模型、温度、技能/频道 JSON、状态机（idle/running/paused/error）
- **AgentGroup / AgentGroupMember / GroupGroupMember**：群组、成员、群组嵌套；7 种 swarmMode（basic/stigmergy/hierarchical/pipeline/consensus/adversarial/mentor）× 4 级 humanControl（observe/approve/copilot/veto）
- **Workflow / WorkflowNode / WorkflowEdge**：画布式工作流（节点类型 agent/group/input/output/condition/human）
- **Conversation / Message**：对话与消息（approved 字段支持人工审批）

### GraphRAG 知识图谱域（5）
GraphRagDocument → GraphRagChunk（分块+embedding JSON）→ GraphRagEntity（6 类实体）→ GraphRagRelation（加权关系）→ GraphRagCommunity（层级社区摘要）

### Ontology 本体域（6）
OntologySchema → OntologyObjectType / OntologyLinkType / OntologyRule → OntologyInstance（含置信度、来源文档）→ OntologyRelation（时态字段 validFrom/validTo/isCurrent）

### SYLVA 学术研究域（5）
ResearchModule（Lean 模块统计）、ResearchPaper（Hilbert/Millennium）、ResearchTheorem、ResearchNote、ResearchSync（仓库同步记录）

### 学术协作生态域（22）
- **专家组**：AcademicPanel / AcademicPanelMember（投票权重、专属 systemPrompt、独立模型）
- **研讨会**：AcademicWorkshop（4 模式、共识度）/ WorkshopRound（argument/rebuttal/synthesis/conclusion 四阶段）
- **稿件流水线**：ManuscriptPipeline（8 阶段状态机）/ PipelineStage（评分+反馈）/ AcademicTask（7 种任务类型）
- **评审团**：AcademicReviewBoard（single_blind/double_blind/open）/ ReviewBoardMember（匿名编号）/ AcademicReview（5 种决定）
- **竞赛**：AcademicCompetition（4 赛制）/ AcademicCompetitor / CompetitionRound
- **导师制**：AcademicMentorship（含苏格拉底式）/ MentorshipSession
- **代码评审**：CodeReviewGroup / CodeReviewMember / CodeReview（5 级 severity）
- **验证**：ValidationGroup（5 验证类型）/ ValidationMember（含 witness）/ ValidationResult（结论+置信度）

### 体系 A 数据模型（31 个，backend/prisma/schema.prisma）
Agent、Framework、Team、Role、Engine、Tool、Group、AgentTemplate、KnowledgeBase、Document、Skill、Integration、Blueprint、Settings、ChatAccount、ChatMessage、WorkspaceTask、BackendConfig、Platform、PlatformConfig、Conversation、Message、Canvas、CanvasRevision、Chariot、ChariotEvent、Workflow、WorkflowExecution、EventLog、Metrics、ApiKey。

---

## 五、服务层说明

### 体系 B：src/lib（11 模块，约 4,600 行）
| 模块 | 行数 | 职责 |
|---|---|---|
| graphrag.ts | 750 | GraphRAG 核心：12 家 LLM 端点注册、callLLM、分块、实体/关系抽取、社区检测与摘要 |
| ontology.ts | 987 | 本体引擎：Schema 解析、实例抽取（LLM）、规则校验、语义查询 |
| platform-adapter.ts | 621 | 平台适配层：统一 ChatMessage 接口，每平台一适配器，SSE 流事件 |
| platform-api.ts | 532 | Dify / Coze 真实 API 客户端（Bearer + SSE 解析） |
| platforms.ts | 215 | 平台注册表：技能、聊天频道、Agent 平台枚举 |
| tools.ts | 590 | 真实工具调用层：DuckDuckGo 搜索、Yahoo Finance 行情、shell 执行 |
| research-llm.ts | 222 | 学术协作专用 LLM 客户端（GLM-5.1 / Kimi，低温度 0.3，直连 fetch） |
| research-prompts.ts | 534 | 9 协作类型 × 20 协作方式的中文 Prompt 模板系统 |
| sylva-parser.ts | 465 | Lean 4 纯文本解析器：定理提取、sorry 检测、证明策略注释 |
| memory.ts | 200 | Agent 记忆层：基于 Message 表的真实数据库存储检索 |
| prisma.ts | 18 | Prisma 单例（libsql adapter） |

### 体系 A：backend/src/services（35 文件）
- **LLM 网关族**：GatewayService（真实 LLM 调用 + SSE 流式）、LLMClient、UnifiedLLMAdapter、LLMProviderRegistry、BackendRouter
- **群智编排族**：SwarmCoordinator、CollabFramework/（InterventionService、HandoffProtocol）、EngineScheduler、IntentClassifier、SkillRouter
- **领域服务族**：AgentService、GroupService、TeamService、RoleService、FrameworkService、SkillService、KnowledgeService、CanvasService、WorkflowService、DialogService、IntegrationService、BlueprintService、MonitorService、TaskService、WorkspaceService、SettingsService、APIKeyService、SpendTracker、PrismaService、InitService
- 另有 adapters/（Claude、DeepSeek、Kimi、Ollama、OpenAI、OpenAICompatible 6 适配器）与 coordinator/AxisMessage、KimiClusterOrchestrator

---

## 六、前端页面地图

### 体系 B（Next.js，8 页）
| 路由 | 行数 | 内容 |
|---|---|---|
| / | 1,363 | 主控制台单页应用：4 视图切换（dashboard / agents / canvas 关系画布 / chat 流式聊天） |
| /graphrag | 825 | 知识图谱工作台（文档上传、实体/关系浏览、查询） |
| /ontology | 1,485 | 本体论工作台（Schema 管理、实例、抽取、查询） |
| /research | 226 | 研究仪表盘（统计总览） |
| /research/modules | 217 | SYLVA 模块浏览器 |
| /research/papers | 250 | 论文/问题库（Hilbert + 千年难题） |
| /research/verification | 253 | 验证状态面板 |
| /research/workspace | 264 | 研究工作区（笔记） |

导航：主页左侧图标栏视图切换；/research 有独立 layout（紫色渐变侧栏：仪表盘/模块/论文/验证/工作区 + 返回首页）。

### 体系 A（frontend/，70 页，App.tsx 约 66 条路由）
- **首页与仪表盘**：/、/dashboard（L0-L3 四级仪表盘组件）、/hierarchical
- **平台与引擎**：/platform、/platforms、/platform-library、/frameworks、/engines、/registry、/ollama、/model-browser
- **Agent 体系**：/agents、/agents/create（AgentCreator + V4）、/agents/collab、/agents/monitor、/agent-contexts
- **群组协作**：/groups、/groups/:id、/collaboration、/teams
- **对话与频道**：/chat、/chat/channels、/chat-accounts、/channels、/sessions、/dialog-center
- **编排与任务**：/workflows、/tasks、/tasks-deep、/task-manager、/scheduler、/processes、/kimi-cluster、/unified
- **监控干预**：/monitoring、/context、/intervention、/events、/events-page、/spend
- **知识与记忆**：/knowledge、/memory、/memory/export、/files、/workspace、/workspaces、/uploads、/ai-search
- **系统集成**：/integration-manager、/integrations、/external-integrations、/webhooks、/api-test、/api-keys、/security、/backups、/settings、/admin/*、/protocol-admin、/blueprints、/handoff、/login

---

## 七、配置与运行指南

### 环境变量（.env.example，43 行）
- **数据库**：`DATABASE_URL=file:./dev.db`（SQLite；prisma.ts fallback 为 `file:./db/custom.db`）
- **LLM Provider Keys（10 家）**：ZHIPU/GLM（智谱）、OPENAI、ANTHROPIC、DEEPSEEK、DASHSCOPE/QWEN（通义）、GEMINI/GOOGLE、MOONSHOT（Kimi）、MISTRAL、XAI、OPENROUTER
- **部署**：NODE_ENV、PORT=3000

### 运行方式（体系 B，README）
```bash
bun install && bun run postinstall   # 生成 Prisma Client
bun run dev                          # next dev
bun run db:push                      # prisma db push --accept-data-loss
```
脚本：dev/build/start/lint/db:push/postinstall。依赖亮点：duck-duck-scrape（搜索工具）、yahoo-finance2（行情工具）、z-ai-web-dev-sdk、uuid。技术栈：Next.js 16.2.9 + React 19.2.4 + Prisma 7（libsql adapter）+ Tailwind CSS 4。

### 运行方式（体系 A）
- backend：`tsx watch src/server.ts`（dev）/ `tsc && node dist/server.js`（prod），Express 4 + helmet + cors + Prisma 5
- frontend：`vite` / `vite build`；集成测试经浏览器 console `runIntegrationTests()`
- electron：`electron .` / `electron-builder`

### 部署
- Railway：railpack 构建，`bun run start`，健康检查 `/`（300s 超时，失败重启 10 次）
- Docker：根 Dockerfile（.dockerignore 已配）
- Vercel：.vercelignore（排除 node_modules/.vite/log）；git 历史含 vercel config 修复提交

### 文档
README.md（体系 B 功能与结构）、worklog.md（初代构建日志：Next.js 16 + Prisma v7 + z-ai-web-dev-sdk，含 Prisma v7 adapter 与 SSE 解析修复记录）、BUILD_GUIDE.md、DOCKERHUB_DEPLOY.md、RAILWAY_OOM_FIX.md、DESIGN.md（29KB 设计文档）、REFACTOR_PLAN.md、PROJECT_SUMMARY.md、CLAUDE.md / AGENTS.md（代理规则）。

---

## 八、最近演进（git log -20 摘要）

```
cba8cc1 Merge main（当前 HEAD，与远程同步）
dcaf6ed 强制添加164张论文图表(PNG)到版本控制
32fdb2d 新增拓扑量子互联网(14篇)与QEC-FTQC(15篇)两大论文系列
509cc4a TOE-SYLVA ↔ 千界花园联动系统——自动同步与研究跟踪
81f9d6f 千年难题研究生态初始化 API
bf05ba3 全部9种学术协作类型集成真实 GLM-5.1 LLM 调用
b2dc752 全面扩展协作组与协作方式 v3.0（无前端）
d2f0771 学术研究群组协作后端 v2.0（无前端）
648491f Merge：学术研究模块整合
83216b9 学术研究工具 v1.0 + GraphRAG + Ontology
c95da0b v4.5：CORS 修复 + L2 编排引擎解析 + vercel 配置
...（更早为 v4.5 系列：PlatformManager 编排控制台化、去 mock、dist 自动构建）
```
累计 170 个提交。演进脉络：**v4.5（体系 A 完善）→ GraphRAG/Ontology 基础设施建设 → 学术协作后端 v1-v3 → 真实 LLM 全面接入 → 千年难题生态 → TOE-SYLVA 联动与论文产出**。

---

## 九、当前状态评估

### 优势
1. **真实数据原则贯彻到位**：mock 已系统性清除；research 域所有执行类端点（研讨轮次、流水线推进、竞赛、辅导、评审、验证）均接入真实 GLM-5.1/Kimi LLM 调用，并附响应解析逻辑（severity/score/conclusion 关键词提取）。
2. **学术协作生态设计完整**：9 类协作形态（专家组/研讨会/流水线/评审团/竞赛/导师/代码评审/验证/任务）在 schema、API、Prompt 模板三层全部落地，含共识度、匿名评审、时态本体等精细设计。
3. **数据模型覆盖面大**：两体系合计 77 个模型；GraphRAG 五层（文档→块→实体→关系→社区）与 Ontology 六件套路径完整。

### 风险与待办
1. **双体系并存的技术债**：体系 A（Express + Prisma 5）与体系 B（Next.js + Prisma 7）模型概念重叠（Agent/Group/Conversation/Message/Workflow 各有一份），长期需明确主次或做数据打通。README 只描述体系 B，容易误导新协作者。
2. **前后端覆盖不均**：research 域 52 个 API 路由仅配 5 个前端页面（协作生态 v2.0/v3.0 提交注明「无前端」），panels/workshops/competitions/mentorships 等 9 类协作尚无独立 UI，只能通过 API 驱动。
3. **SQLite 天花板**：协作生态的多轮 LLM 写入（大文本 content/history JSON）在并发下存在锁竞争风险；模型中大量 JSON 字符串字段（embedding、entityIds 等）不利于查询。
4. **仓库卫生**：根目录混放 TOE-SYLVA 学术工作文件（.md/.csv/.png/.tex，100+ 未跟踪文件）与历史误建的 `C:Users一梦...` 字面目录，建议清理或迁入子目录；`.env` 未建立（仅 .env.example）。
5. **数据库路径与迁移隐患**：.env.example 指向 `file:./dev.db`，prisma.ts fallback 为 `file:./db/custom.db`；仅一次 init 迁移（20260612），后续 40+ 模型演进依赖 `db:push --accept-data-loss`，有数据丢失风险。

### 总体判断
项目处于「**体系 A 成熟稳定（v4.5 去 MOCK 完成、70 页控制台），体系 B 学术生态后端完备、前端滞后**」的阶段。v4.5 的 61 路由记录已过时：仅体系 B 的 Next.js API 即达 74 路由 / 133 端点，体系 A 另有约 302 端点。下一步性价比最高的工作：(a) 为 research 协作生态补前端页面；(b) 明确双体系合并/分工策略；(c) 仓库文件整理。

---

*报告完。分析全程只读，未修改任何项目代码、未执行 git 写操作、未启动服务器。*
