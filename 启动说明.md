# 千界花园（bloom bloom garden）· 移植包使用说明

**打包日期**: 2026-08-12 · **来源**: C:\Users\一梦\Documents\kimi\workspace（bloombloomgarden）

## 这是什么

群智协同系统「千界花园」的完整可运行移植包：Next.js 16 应用（74 个 API 路由：agents/chat/conversations/groups/workflows/graphrag/ontology/research 八大分组，research 域 52 个端点）+ 后端/前端/Electron 体系 + Prisma 数据库（dev.db 含真实使用数据）+ **node_modules 全部依赖（开箱即用，无需 npm install）**。

LLM 通道已接入 Kimi 网关（`src/lib/kimi-gateway.ts`）：Kimi 优先、GLM 兜底；research/panels 群智评议已修复 4096 截断（maxTokens=12288）。

## 启动（在任意 Kimi Work 中）

```bash
cd <解压目录>
"C:\Users\一梦\AppData\Local\Programs\kimi-desktop\resources\resources\runtime\node.exe" node_modules/next/dist/bin/next dev -p 3001
```

- 打开 http://localhost:3001
- Kimi 网关密钥：Kimi Work 运行环境自动注入 `KIMI_API_KEY` / `KIMI_BASE_URL` 环境变量，**无需配置**
- 数据库：`dev.db`（SQLite/Prisma 7 + libsql）已在包内，无需初始化

## 验证 LLM 可用

```bash
curl -N -X POST http://localhost:3001/api/chat -H "Content-Type: application/json" -d '{"message":"你好"}'
# SSE 流式返回真实模型响应即正常
```

群智评议入口：`POST /api/research/panels`（建组→加成员→execute），详见包内《千界花园_API使用手册.md》。

## 包内容说明

- `src/`：Next.js 应用源码（app/api/lib/services）
- `backend/`、`frontend/`、`electron/`：体系 A（Express+Vite+Electron）
- `prisma/`：schema 与迁移
- `node_modules/`：全部依赖（804MB，已含 Prisma Client 生成物）
- `dev.db`：真实数据库
- `千界花园_API使用手册.md`、`千界花园_项目分析报告.md`：文档
- 配置：package.json / next.config.ts / tsconfig.json / prisma.config.ts / .env.example 等

## 备注

- `.env` 中 GLM/Zhipu key 已失效可忽略；Kimi 网关由环境变量驱动
- npm 不在系统 PATH 是正常现象，请用上文的 node 完整路径
- 如需构建：`node node_modules/next/dist/bin/next build`（.next 缓存未含，首次 dev 会自动生成）
